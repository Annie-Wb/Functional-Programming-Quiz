# Quiz 02 — Parallelism

Exercises

1. `par` and `pseq`
   - Write a `parallelPairSum` that uses `par` and `pseq` to evaluate two independent computations in parallel.

2. `Control.Parallel.Strategies`
   - Implement a function `parallelSum :: [Int] -> Int` that splits the list and sums parts in parallel.